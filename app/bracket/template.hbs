<nav class="round-nav">
  {{#each model.rounds as |round|}}
    <div>
      {{#link-to 'bracket.round' round.id}}{{round.title}}{{/link-to}}
    </div>
  {{/each}}
</nav>
<div class="round">
  {{outlet}}
</div>
