<div class="round">
  {{outlet}}
</div>
<nav class="round-nav">
  {{#each rounds as |round|}}
    <div>
      {{#link-to 'bracket.round' round.id}}{{round.title}}{{/link-to}}
    </div>
  {{/each}}
</nav>
